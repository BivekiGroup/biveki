# ===== General =====
NODE_ENV=production
PORT=3000

# ===== Database (Managed / Cloud PostgreSQL) =====
# Full connection string to your managed Postgres (required)
# Example: DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require
DATABASE_URL=

# (Optional) If you ever run local Postgres, you can define these, but they are not used
# POSTGRES_DB=biveki
# POSTGRES_USER=biveki
# POSTGRES_PASSWORD=change_me

# ===== Auth / Admin =====
JWT_SECRET=please_change_me
# Comma-separated admin emails (grants isAdmin=true on first registration)
ADMIN_EMAILS=admin@example.com

# ===== App URLs =====
# Для SSR-фетчей внутри контейнера используем INTERNAL_API_URL
# По умолчанию 127.0.0.1:3000 в compose
# INTERNAL_API_URL=http://127.0.0.1:3000

# ===== SMTP (optional; leave empty to disable mail) =====
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
NOTIFY_TO=
